package com.gpstracker.server.model.internal;

/**
 * An enum that define the processing stages in the processing.
 */
public enum ProcessingStageType {
    
    REQUEST_RECIEVED,
    INPUT_REQUEST,
    MESSAGE_PROCESSING,
    RESPONSE_GENERATOR;
}
