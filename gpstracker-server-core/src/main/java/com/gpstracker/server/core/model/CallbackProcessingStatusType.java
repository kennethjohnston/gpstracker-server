package com.gpstracker.server.core.model;

/**
 * An enumeration to define the callback processing status types.
 */
public enum CallbackProcessingStatusType {    
    PROCESSING,
    COMPLETE,
    COLLECTED;
}
